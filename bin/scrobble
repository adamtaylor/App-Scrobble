#!/usr/bin/env perl

use warnings;
use strict;

# ABSTRACT: Command line scrobbling client
# PODNAME: App::scrobble
# VERSION

use App::Scrobble;

my $app = App::Scrobble->new_with_options();

$app->scrobble();
